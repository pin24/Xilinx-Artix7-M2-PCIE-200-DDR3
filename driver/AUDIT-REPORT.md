# AUDIT-REPORT — повторный аудит кода (до/после правок)

> Сессия: 2026-09-11. Область: `driver/` (C + cmd + py), `pytorch_layer/`, `scripts/` (TCL, чтение), `rtl/` (чтение). Полные сборки FPGA не выполнялись.

## 1. Метрики «до/после»

| Проверка | До правок | После правок | Комментарий |
|---|---|---|---|
| `py_compile` всех .py проекта | 71 checked, **0 ошибок** | 71 checked, **0 ошибок** | регрессий нет |
| `ternary_sw` тесты | 10/10 passed | 10/10 passed | эталонная арифметика |
| `verify_fpga_backend.py` (mock) | OK (4 пути идентичны, diff 0.0) | OK | CPU/FPGA-mock/RES0-DDR3 совпадают |
| `ternary_dot_layer.py` self-check | OK (max diff 4.77e-07) | OK | точность троичного dot |
| `driver/emulate_test.py` | 6/6 PASS, **но на легаси-карте** | **6/6 PASS на канонической DFX-карте** | E-05 |
| `driver/edge_cases.py` | 6/6 PASS, **но на легаси-карте** | **6/6 PASS на канонической DFX-карте** | E-05 |
| Драйвер: сборка | ok (v1.1.3.0), но **BSOD при загрузке** | **Build FULL SUCCESS (v1.1.4.0)**, загрузка без BSOD | E-01 |
| Драйвер: поведение при DDR3-запросах | обращение к BAR2 → **фатальный AER → BSOD 0x124** | `STATUS_DEVICE_NOT_CONNECTED`, без обращения к железу; тест **SKIP** | E-04 |
| Компилятор (cl) предупреждения | сборка чистая | сборка чистая | новых предупреждений нет |

## 2. Находки статического аудита (драйвер/инфраструктура)

| Область | Проверено | Находка | Статус |
|---|---|---|---|
| `driver.c` — границы буферов | `EvtIoRead/EvtIoWrite`: `barOffset >= BarLen`, `bufferLen > BarLen - barOffset` | корректно; при Bar2Length=0 → быстрый отказ | ✅ ок |
| `driver.c` — маршрутизация BAR | второй BAR принимался как DDR3 без проверки | **дефект → FIX-11** | ✅ исправлено |
| `driver.c` — утечки/размотка | `EvtDeviceReleaseHardware`, очистка при ошибках маппинга | корректно | ✅ ок |
| `driver.c` — IRQL/DPC | все WDF-вызовы в EvtIoRead/Write на PASSIVE_LEVEL, очереди sequential | корректно | ✅ ок |
| `build.cmd` | точка входа, упаковка, штамп даты | дефекты → FIX-8/9/10 | ✅ исправлено |
| `XDMA.inx` | секции, версия, [SourceDisks*] | дефект → E-02 | ✅ исправлено |
| `install.cmd`/`uninstall.cmd` | разбор `pnputil`, нумерация, чистка | дефект → E-06 | ✅ исправлено |
| `clean.cmd` | удаление build/build_tmp/cert | корректно | ✅ ок |
| `security_cookie.c` | дубликаты символов | корректно (обход C2373) | ✅ ок |

## 3. Находки статического аудита (Python-обвязка)

| Модуль | Проверено | Находка | Статус |
|---|---|---|---|
| `xdma_driver.py` | маршрутизация адресов, DMA-чанки (1 МиБ), выравнивание 4Б, планировщик (кольцо mod 256, wrap дескрипторов) | логика корректна; **шапка противоречила карте** → E-08 | ✅ исправлено |
| `fpga_backend.py` | конверсии 48-бит ↔ float, дерево сложений, пути CPU/FPGA | корректно | ✅ ок |
| `icap_load.py` | парсинг .bit/.bin, LE-представление слов, протокол GO→READY→DATA→STOP | корректно (совпадает с `icap_ctrl.sv`) | ✅ ок |
| `dfx_swap.py` | биты DFX socket, quiesce/resume/verify, glob | корректно | ✅ ок |
| `monitor_temp.py` | чтение XADC, обработка invalid | **дефект (ложные 0 °C)** → FIX-13 | ✅ исправлено |
| `flash_write.py` | заглушка | ожидаемо (не реализовано) | ⚠ зафиксировано |
| `driver/emulate_test.py`, `edge_cases.py` | карта/декодеры | дефект → E-05 | ✅ исправлено |

## 4. Находки статического аудита (Vivado-скрипты, чтение)

| Файл | Проверено | Вывод |
|---|---|---|
| `scripts/build_dfx.tcl` | шаг 2d: `pf0_bar0_scale=Megabytes`, `pf0_bar0_size=128`; отчёт BAR/MSI-X; парсинг аргументов (BUG-033) | ✅ корректно; BAR0=128 МБ соответствует документации |
| `scripts/xdma_ddr3_dfx_bd.tcl` | адресация BD, MSI-X на BAR2, апертуры RP | ✅ согласовано с ADDRESS_MAP |
| `scripts/post_bd_dfx.tcl`, `constraints/*` | констрейны, pblock, timing-исключения | ✅ без новых находок (историч. BUG-022/026/038 закрыты) |

**Вывод по FPGA-части**: расхождение «1 МБ vs 128 МБ» — не дефект скриптов, а признак устаревшего битстрима на плате (R-01).

## 5. Сравнение «число находок»

| Категория | До | После |
|---|---|---|
| Дефекты, требующие исправления (в коде/скриптах) | **6** (E-01…E-06), из них 2 вызывали BSOD | **0** |
| Дефекты документации/консистентности | 2 (E-07/E-08) | 0 |
| Открытые внешние вопросы | — | 9 рисков (R-01…R-08, +A-01…A-03) |

## 6. Повторный аудит после правок

- Все правки перечитаны в диффе (`git diff --stat`: 11 файлов, +345/-125).
- Повторный прогон всех Python-проверок и эмуляторов — зелёный (см. §1).
- Пересборка драйвера с чистого состояния — успешна; загрузка/перезапуск устройства без BSOD.
- Новых находок повторный аудит не выявил.
