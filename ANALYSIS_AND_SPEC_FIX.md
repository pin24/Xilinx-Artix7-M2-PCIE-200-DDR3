# Анализ проекта XDMA_DDR3 и ТЗ на исправление ошибок

Дата: 2026-08-27. Объект: `C:\A7_M2\EXAMPLES\XDMA_DDR3` — троичный FP-ускоритель TFloat48
(Artix-7 XC7A200T, M.2 PCIe x4, XDMA + DDR3 MIG, Vivado 2021.2).

Состояние на момент анализа: git-репозиторий чистый (1 коммит `2493f69`), BAR0 = 128 MB
задан в `scripts/build_all.tcl` (шаг 4, `CONFIG.pf0_bar0_size {128}`) — **принято, учтено в ТЗ**.
Последняя успешная сборка битстрима — 25.08.2026 (`.bit/.bin/.mcs` в `vivado.log`),
но каталог `m2_artix7_xdma_ddr3/...runs/` впоследствии очищен — битстрима в проекте сейчас нет.
Попытка пересборки 27.08 упала (см. B-1). Вердикт: **собранный 25.08 битстрим неработоспособен
по функциональности ускорителя** (находка A-1), пересборка обязательна после исправлений.

---

## Часть 1. Результаты анализа

### A. Критические ошибки RTL (блокируют работу на железе)

**A-1. Top-уровень: `axi_aclk` / `axi_aresetn` не существуют — тактирование ускорителя и ICAP висит в воздухе.**
- Файл: `rtl/integration/xdma_ddr3_core_top.sv:83,93` — `u_tdot` подключён к `axi_aclk`,
  `axi_aresetn`, но эти имена в модуле **не объявлены и не подключены ни к какому порту BD**.
  Verilog молча создаёт неявные 1-битные провода без драйвера (константа) — синтез проходит,
  но вся логика ускорителя и ICAP в собранном битстриме мертва/оптимизирована.
- Обёртка BD (`m2_artix7_xdma_ddr3.gen/.../hdl/xdma_ddr3_wrapper.v`) **не экспортирует ни одного
  тактового порта** — взять клок снаружи неоткуда.
- Подтверждение из логов: CRITICAL WARNING SmartConnect `xdma_ddr3_axi_smc_0`: «S01_AXI/S02_AXI
  do not share a common clock domain» (`vivado_9916.backup.log:102-103`).
- Это корневая причина: M_AXI_TDOT заявлен с FREQ_HZ 125000000, но реальный клок не подведён.

**A-2. AXI-Lite slave `tdot_axi4.sv`: потеря транзакций при разнесённых AW/W фазах → зависание шины.**
- Файл: `rtl/integration/tdot_axi4.sv:156-186`.
- Запись регистрируется только при `S_AXI_WVALID && S_AXI_AWVALID && !bvalid` **в одном такте**.
  Если AW принят в такте N (awready пульс), а W придёт в такте N+1 — AWVALID уже снят,
  условие не выполнится, `bvalid` не будет выставлен никогда → interconnect повиснет навсегда.
  Работает «на удаче» только потому, что Xilinx interconnect выставляет оба VALID одновременно.
- Аналогично: если `bvalid` ещё поднят от предыдущей записи, новая запись молча теряется.
- Тот же шаблон в `tdot_axi_lite.sv:100-138` и в ослабленном виде в `icap_ctrl.sv:50-68`.

**A-3. Известный баг «GO затирает N_IN» — подтверждён, находится в `tdot_axi_lite.sv` (не в `tdot_axi4.sv`).**
- Файл: `rtl/integration/tdot_axi_lite.sv:119-122`:
  ```systemverilog
  4'd0: begin
      go_reg    <= S_AXI_WDATA[0];
      n_in_reg  <= S_AXI_WDATA[16:8];   // <-- побочный эффект записи CTRL
  end
  ```
  Запись `CTRL=0x1` (GO) записывает в N_IN значение `WDATA[16:8]` = 0, затем `n_in_eff`
  трактует 0 как NUM_MAC. В используемом в интеграции `tdot_axi4.sv:169-171` этого побочного
  эффекта нет. Требуется исправить/выбросить дубль, чтобы хост-протокол был единым.

**A-4. Потеря битов [31:16] результата в регистрах.**
- Файл: `rtl/integration/tdot_axi4.sv:213-214,221-222,487-489` (и `tdot_axi_lite.sv:165-166,173-174,184-185`).
- RES0 читается как `result[15:0]`, RES1 как `result[47:32]`; средние 16 битов результата
  недоступны ни в одном регистре (RES0/RES1 и зеркала CORE_RES0/1). Через память результат
  пишется полностью (`wr_data = {16'h0, core_result}`) — дефект только в регистровом интерфейсе.

**A-5. ICAP-контроллер `icap_ctrl.sv` — не соответствует протоколу ICAPE2 (UG470).**
- Файл: `rtl/integration/icap_ctrl.sv`.
- (a) `CSIB` удерживается LOW всё время ST_SEND/ST_WAIT (строки 159-200) — ICAP «съедает»
  повторяющиеся старые слова между записями хоста; поток конфигурации разрушается.
- (b) Нет преобразования порядка байтов: битстрим `.bin` для ICAP X32 требует byte-swap
  (bit-reversed) формат; контроллер пушит слова как есть. Согласование с `pytorch_layer/icap_load.py`
  не показано.
- (c) `CLK = axi_aclk` = 125 МГц превышает макс. частоту ICAPE2 (100 МГц для Artix-7);
  `clk_en` = 1 из 2 тактов (62.5 МГц) не спасает — ICAP сэмплирует каждый фронт CLK при CSIB=0.
- (d) Нет процедуры DESYNC/DESYNC-слов, нет контроля статуса (O не подключён), нет таймаутов.

**A-6. GO при BUSY / гонки контроллера.**
- Файл: `rtl/integration/tdot_axi4.sv:408,438-444`.
- Новый GO во время выполнения (`busy_q=1`) перезапускает FSM (сброс cstate в CS_RD_DATA,
  перезапуск чтений) без сброса FIFO и без прекращения текущего burst-чтения/записи:
  FIFO рассинхронизируется (в него попадают данные смешанных транзакций), AR/AW каналы
  продолжают выдавать транзакции от старого запуска. Нужен запрет GO при BUSY либо
  корректный полный сброс (purge FIFO, сброс rd/wr FSM).

### B. Ошибки сборки (скрипты, BD, констрейны)

**B-1. `scripts/build_all.tcl:74-79` — ручное удаление каталогов runs после `reset_run` ломает следующий запуск.**
- 27.08 сборка упала: `ERROR: [Common 17-69] Run 'synth_1' needs to be reset before launching`
  (`build/vivado_build.log`). Причина: `file delete -force ...runs/synth_1|impl_1` оставляет
  проект в несогласованном состоянии. Удалить строки 77-79 (reset_run достаточно).

**B-2. Расхождение карты адресов документ ↔ BD.**
- README/INTEGRATION_REPORT: TDOT 0x4400_0000, ICAP 0x4600_0000, DEBUG 0x4700_0000.
- `build_all.tcl:48-63` (фактическое): GPIO 0x4000_0000, TDOT 0x4000_1000, ICAP 0x4000_2000
  (внутри BAR0 128 MB). Документацию и хост-код привести к фактическому виду; зафиксировать
  одну таблицу как источник истины.

**B-3. Wrapper BD устарел/неконсистентен.**
- В wrapper присутствуют посторонние порты `M_AXI_ICAP` (64-бит мастер — след старого подхода
  «ICAP как мастер») и `S_AXI_XADC_REGS` (slave есть в BD, но top его не подключает), при этом
  нет порта клока (см. A-1). После правок BD перегенерировать wrapper и выверить все порты.

**B-4. Констрейны `constraints/xdma_ddr3_pins.xdc`.**
- Диф. пара PCIe refclk: назначен только `clk_p` (F10, строка 18); `diff_clock_rtl_0_clk_n`
  не назначен вообще — дополнить (парный пин платы, обычно F9/F11).
- CRITICAL WARNING impl: `Timing 38-472` — REFCLK IDELAYCTRL MIG не достигается клоком
  (200 МГц refclk MIG не разведён/не констрейнчен) — проверить `xdma_ddr3_early.xdc` и
  конфигурацию MIG (должен прийти от системного клока платы).
- CRITICAL WARNING impl: конфликт LOC GT-каналов (PCIE_X0Y0.xdc против платных пинов,
  `vivado_9916.backup.log:1138-1144`) — lane-мэппинг x4 для платы проверить по schematic.
- Нет `create_clock` для PCIe refclk в пользовательском XDC — убедиться, что клок приходит
  от IP (обычно автоматически) и его период задан.

**B-5. Модули, заявленные в документации, но не интегрированные.**
- `debug_mon.sv` отсутствует в `rtl/integration/` (в списке файлов его нет) — порт
  S_AXI_DEBUG_REGS не создан; README обещает 0x4700_0000.
- `xadc_temp.sv` добавляется в сборку (`build_all.tcl:26`), но в `xdma_ddr3_core_top.sv`
  не инстанцирован и не подключён к S_AXI_XADC_REGS — мёртвый код; `monitor_temp.py`
  без этого не заработает.

### C. Хост-софт / драйвер

**C-1. Карта регистров и базовые адреса в Python-слое не сверены с фактической BD** —
`pytorch_layer/fpga_backend.py` использует `xdma_driver.py`; база TDOT-регистров должна
соответствовать `0x4000_1000` (BAR0 128 MB). Требуется прогон на плате.

**C-2. `pytorch_layer/icap_load.py`** — обязан выдавать ICAP-формат (byte-swap 32-бит слов,
пропуск заголовка .bin, выравнивание); согласовать с исправленным `icap_ctrl.sv` (A-5).

**C-3. `xdma_driver_win_src_2017/DRIVER_DEVLOG.md`** — известные исправленные проблемы
драйвера зафиксированы; открытый пункт — инсталлятор и подпись (README «Открытые задачи»).

### D. Что в порядке (подтверждено)
- Тайминг последней полной имплементации: **WNS +0.370 нс, TNS 0, WHS +0.034 нс — все
  констрейны выполнены** (`scripts/timing_full.rpt`).
- Симуляционная верификация логики: `verify_tdot_axi4.py` PASS (16/32), `rtl_vs_arith48.py`
  PASS, `verify_fpga_backend.py` PASS (mock) — арифметика и протокол ядро↔память корректны
  в симуляции.
- `write_bitstream` в `build_all.tcl` уже исправлен на `.bit` + `-bin_file` (ошибка из README
  устранена; 25.08 .bit/.bin/.mcs успешно созданы).
- BAR0 128 MB задан (учитывать в хосте: BAR0 покрывает GPIO/TDOT/ICAP, DDR3 за XDMA M_AXI
  по 0x8000_0000).

---

## Часть 2. ТЕХНИЧЕСКОЕ ЗАДАНИЕ на исправление ошибок

### 2.1. Цель
Привести проект в состояние, пригодное для работы на плате: ускоритель TFloat48 доступен
хосту через PCIe (BAR0 128 MB), регистры/каналы AXI соответствуют спецификации, ICAP
позволяет перезагрузку битстрима, сборка воспроизводима одной командой.

### 2.2. Объём работ и приоритеты

**P0 — блокирующие функциональные дефекты железа**

| № | Работа | Файлы | Критерий приёмки |
|---|--------|-------|------------------|
| P0-1 | Экспортировать клок/ресет из BD: создать BD-порт `aclk_out` (O), подключить к `xdma_0/axi_aclk` (+ `axi_aresetn`); подключить в `xdma_ddr3_core_top.sv` к `u_tdot`; устранить неявные провода `axi_aclk/axi_aresetn` | BD (tcl-скрипт), `xdma_ddr3_core_top.sv` | В синтезе 0 предупреждений о неявных сигналах; SmartConnect DRC «no common clock» исчез; все Clock Domains в `report_clock_networks` корректны |
| P0-2 | Переписать AXI-Lite write/read каналы `tdot_axi4.sv` по стандартному шаблону (PG055/UG585): независимые фазы AW/W/B, latched awaddr при AW+W, `bvalid` без потери при повторных записях | `tdot_axi4.sv` | Симуляция: случайные задержки AW/W (0..10 тактов) — 1000 записей без зависаний; чтения с back-to-back AR — данные корректны |
| P0-3 | Устранить побочный эффект записи CTRL в `tdot_axi_lite.sv` (удалить `n_in_reg <= WDATA[16:8]`); унифицировать регистровую карту двух модулей, дубль либо удалить, либо пометить deprecated | `tdot_axi_lite.sv` | Запись CTRL=0x1 не меняет N_IN (проверка в xsim) |
| P0-4 | Переработать `icap_ctrl.sv` по UG470: CSIB=LOW только на длительность одного слова; корректный формат X32 (byte-swap либо конвертация на хосте в `icap_load.py`); тактирование ≤100 МГц (ввести делённый клок MMCM или стробировать на 62.5 МГц корректно); добавить DESYNC и чтение статуса | `icap_ctrl.sv`, `pytorch_layer/icap_load.py` | Симуляция с ICAPE2 UNISIM: последовательность 0xAA995566… принимается без пропусков; на плате — успешная перезагрузка тестового битстрима через ICAP |
| P0-5 | Запрет/корректная обработка GO при BUSY: игнорировать GO при busy_q=1 (или полный сброс FSM+FIFO+AR/W каналов) | `tdot_axi4.sv` | Тест: GO во время BUSY — либо игнор, либо корректный перезапуск с чистым FIFO; результат совпадает с эталоном |

**P1 — сборка и интеграция**

| № | Работа | Файлы | Критерий приёмки |
|---|--------|-------|------------------|
| P1-1 | Убрать ручное `file delete` каталогов runs из `build_all.tcl` (строки 77-79); оставить `reset_run` | `build_all.tcl` | Повторный запуск `build_all.tcl` дважды подряд — успешно |
| P1-2 | Синхронизировать карту адресов: фактическая (GPIO 0x4000_0000, TDOT 0x4000_1000, ICAP 0x4000_2000, DDR3 0x8000_0000, BAR0 128 MB) — обновить README, INTEGRATION_REPORT, `fpga_backend.py`, `xdma_driver.py` | доки + pytorch_layer | Одна таблица адресов во всех источниках; хост-скрипт читает/пишет по фактическим адресам |
| P1-3 | Восстановить полные биты результата в регистрах: RES0=[31:0], RES1=[47:32] (и зеркала CORE_RES) | `tdot_axi4.sv`, `tdot_axi_lite.sv`, хост | Хост читает 48-бит результат из регистров == результат в DDR3 |
| P1-4 | Констрейны: назначить `diff_clock_rtl_0_clk_n`; устранить REFCLK-IDELAYCTRL предупреждения MIG; проверить GT lane LOC по схеме платы; убедиться в наличии/корректности `create_clock` PCIe refclk | `constraints/*.xdc` | 0 CRITICAL WARNING в synth/impl (кроме известных безобидных), битстрим собирается |
| P1-5 | Выверить wrapper BD: убрать legacy-порт M_AXI_ICAP, либо подключить S_AXI_XADC_REGS + `xadc_temp.sv` в top (для monitor_temp.py), либо удалить XADC из BD | BD, `xdma_ddr3_core_top.sv` | Все порты wrapper подключены в top; нет неиспользуемых интерфейсных портов |
| P1-6 | Решение по debug_mon: добавить модуль+порт 0x4000_3000 (реалистичная альтернатива обещанному 0x4700_0000) либо исключить из документации | BD/RTL/доки | Документация == железо |

**P2 — верификация и board bring-up**

| № | Работа | Критерий приёмки |
|---|--------|------------------|
| P2-1 | Регресс xsim: все существующие тестбенчи + новые кейсы (разнесённые AW/W, GO при BUSY, N_IN=0/1/NUM_MAC/NUM_MAC+1) — PASS | 0 mismatch |
| P2-2 | Пересборка `build_all.tcl` → .bit/.bin/.mcs; сохранить артефакты вне runs (например `build/artifacts/`), т.к. runs может очищаться | Файлы .bit/.bin/.mcs созданы, timing WNS≥0 |
| P2-3 | Board bring-up: перечисление PCIe (lspci/диспетчер), LED GPIO-тест, чтение ID/версии через регистры TDOT | Устройство видно, LED мигают, регистры отвечают |
| P2-4 | DMA-тест DDR3: WriteBlock/ReadBlock через XDMA M_AXI (test_xdma.exe), затем прогон dot N=32 из DDR3 с сверкой с эталоном | Данные DDR3 совпадают; dot результат == arith48 эталон ±0 |
| P2-5 | ICAP-перезагрузка на плате: `icap_load.py` с тестовым битстримом | FPGA перезагружается без JTAG |
| P2-6 | Драйвер: сборка `driver/build.cmd`, тест `test_xdma.exe`, инсталлятор (README «Открытые задачи») | Установленный подписанный драйвер, тест проходит |

**P3 — документация**
- Обновить README/INTEGRATION_REPORT: карта адресов, регистровая карта (включая RES0/1 48-бит),
  протокол ICAP, известные ограничения, статус задач.
- Зафиксировать «одну таблицу адресов» как источник истины в `docs/ADDRESS_MAP.md`.

### 2.3. Порядок выполнения
1. P0-1 → P0-2 → P0-3 (RTL-стабилизация), затем P1-1..P1-3 — первая пересборка и симуляция.
2. P0-4 (ICAP) параллельно, с отдельным тестбенчем ICAPE2.
3. P1-4..P1-6 — чистка интеграции, вторая пересборка.
4. P2-1..P2-6 — верификация и bring-up.

### 2.4. Риски и допущения
- Реверс-инжиниринг пинов платы (refclk_n, GT lanes) ведётся по логам и типовым M.2-платам;
  при доступности schematic платы — сверить. Интернет-источники: UG470 (ICAP), PG054 (MIG),
  PG195 (DMA/Bridge), DS181 (Artix-7 даташит, частоты ICAP).
- Частота axi_aclk 125 МГц при NUM_MAC=32 и текущей логике умножителя проходит тайминг с
  запасом +0.37 нс; изменение шаблона AXI-Lite не должно ухудшить (малый критический путь).
- Битстрим 25.08 использовать только как JTAG-загрузчик первой прошивки; функционально
  ускоритель в нём неработоспособен (A-1) — не тратить время на отладку по нему.

### 2.5. Критерии завершения работ (Definition of Done)
1. `build_all.tcl` проходит от начала до конца, создаёт .bit/.bin/.mcs, тайминг met, 0 CRITICAL WARNING.
2. Симуляционный регресс зелёный; хост (mock → плата) вычисляет dot с корректным результатом.
3. На плате: PCIe enumeration, LED, DMA DDR3, dot через регистры, ICAP-перезагрузка — все пункты P2 выполнены.
4. Документация соответствует фактическому состоянию.
